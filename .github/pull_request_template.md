<!-- Adding your own plugin? Everything below is optional — CI checks it all
     anyway, and an addition merges on its own once CI is green. -->

**What it does, in a sentence:**

**Where the code is:**

---

Adding an entry merges automatically when CI passes. Editing or removing an
entry that is already listed does not: an edit can point an id somebody already
installed at a different repository, so those wait for a person.
